# 知识中台 · 增量实施计划（M0 → M3）

> 编制：2026-09-21
> 上游需求基线：[docs/development-plan.zh-CN.md](./development-plan.zh-CN.md)（M0–M5 全量方案、47 项功能、依赖链、验收口径、风险清单均不动，本文件仅负责落地）。
> 起点：当前仓库只有 M0 空骨架——后端 FastAPI 起 + 3 个 IRI/health 接口，ORM/迁移/语义层全空；前端仅 `createApp(App)`，无路由/Pinia/i18n/UI 库/API 客户端；samples 只有 README。
> 团队口径：沿用上游 §0 假设（1 产品/架构 + 1 语义工程 + 3 后端 + 2 前端 + 1 测试，S1 起介入）。周期按该规模给；单人按 ×1.7 系数（与上游一致）。
> 边界：本文件只覆盖到 **M3（v0.1.0，主流程贯通）**，即 P0 22 项全部上线。M4/M5 见上游文档，不在本计划展开。

---

## 0. 现状基线（动手前必须读）

| 层       | 已有                                                                                                      | 缺失（M0 必须补）                                                                                                                           |
| -------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 后端骨架 | `app/main.py`（FastAPI + SPA 托管）、`app/iri.py`（name/namespace 校验 + build_iri）、`tests/test_iri.py` | ORM 模型、Alembic 迁移、`core/`（配置/错误码/日志）、`semantics/` 适配层、`domain/` 领域服务、`api/v1/` 资源路由、`connectors/`、`schemas/` |
| 前端骨架 | `App.vue`（空壳）、`main.ts`、`locales/zh-CN.json`（仅 1 个 key）                                         | 路由、Pinia、vue-i18n、Naive UI、API 客户端、19 个路由页占位、`stores/components/views/router/api/i18n` 目录全是 `.gitkeep`                 |
| 样例库   | `samples/README.md`（M2 交付要求）                                                                        | `seed.sql` + `enterprise_demo.sqlite`（M2 交付）                                                                                            |
| 工程规范 | Makefile / ESLint flat config / Ruff+Mypy / commitizen+husky / prettier / .editorconfig 全已落地          | CI workflow（`.github/workflows/` 需核验是否已配）、`pip-licenses` 许可证检查、i18n key 一致性脚本                                          |

**硬规则提醒**（上游 §3，本计划全程遵守）：文案不硬编码（vue-i18n）、后端只返回错误码+结构化参数（不返回中文句子）、状态传枚举码（DRAFT/PUBLISHED、PASS/VIOLATION/NA/NOT_RUN）、四态校验不失真、版本冻结、demo 不入库、依赖只 MIT/BSD/Apache/PSF、Python 必写类型注解。

---

## 1. 决策锁定（本计划已拍板，开工不再悬置）

| 项         | 决策                                                                                                | 依据                                                                                                       |
| ---------- | --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| UI 库      | **Naive UI**                                                                                        | 上游 §3.1.1 已论证：表格一体化 + TS 源码 + 运行时 themeOverrides，与 demo 观感最接近。本计划不再为选型暂停 |
| 产品显示名 | 沿用仓库名 `knowledge`，产品名待定不影响开发（上游 §11 第 2 项可后置）                              | 不阻塞 M0                                                                                                  |
| 后端       | Python FastAPI + rdflib/pySHACL/owlrl，OWL-DL 走可选 Jena sidecar                                   | 上游 ADR-0001 已定                                                                                         |
| i18n       | 三层全做（L1 界面 / L2 API 消息+枚举 / L3 领域语义双语），M0 建立 L1/L2 骨架，L3 随 M1 资源模型落地 | 上游 §5                                                                                                    |
| 前后端契约 | FastAPI `openapi.json` → CI 用 openapi-typescript 生成 `web/src/api/schema.d.ts`，禁止前端手写类型  | 上游 §4                                                                                                    |
| 并发控制   | 所有写接口强制 `If-Match: <revision>`，冲突返回 `KG.REVISION_CONFLICT`（409）                       | 上游 §4.3                                                                                                  |
| 执行       | 首版 FastAPI 后台任务（asyncio + 运行记录落库 + 状态机），M3 不引 Celery                            | 上游 §3.2                                                                                                  |

---

## 2. 里程碑总览（贴合当前空骨架）

| 里程碑                         | 净工期 | 日历区间                | 上游主流程  | 交付物                                                                                  | 验收                         |
| ------------------------------ | ------ | ----------------------- | ----------- | --------------------------------------------------------------------------------------- | ---------------------------- |
| **M0** 基座+语义引擎+i18n 骨架 | 3 周   | 2026-09-21 ~ 2026-10-16 | 前置        | 元数据库+迁移、FastAPI 契约骨架、状态机+修订号、IRI 治理、语义 PoC、i18n L1/L2、CI 基线 | H-00~H-04                    |
| **M1** 语义资产闭环            | 5 周   | 2026-10-19 ~ 2026-11-20 | S1 S2 S3 S4 | 实体类型/关系/业务域/本体装配+设计器、资产层 8 项校验、i18n L3                          | A01~A06 A17                  |
| **M2** 数据接入与映射          | 4 周   | 2026-11-23 ~ 2026-12-18 | S5 S6       | 数据源连接、元数据采集与目录、受限样例、映射 5 步向导+编译预览、samples 样例库          | A10 A11 A12 A15 A18          |
| **M3** 抽取与图谱闭环 ★v0.1.0  | 5 周   | 2026-12-21 ~ 2027-01-22 | S7 S8 S9    | 抽取引擎四阶段、图谱版本与结果集、图谱浏览、实体实例、证据追溯、实例校验报告、工作台    | A13 A14 A20 + 4 项自动化回归 |

到主流程贯通：17 净周 ≈ 4.5 个月（与上游 §4 阶段总览一致，日历区间已预留国庆/春节缓冲）。

---

## 3. M0 · 基座 + 语义引擎 + i18n 骨架（3 周）

### 3.1 目标

消灭"数据在内存、刷新即重置"，验证语义引擎能真跑，把 i18n 骨架钉死。**M0 结束必须能演示**：写一条资源 → 发布一个版本 → 重启进程后仍在 → 改名字后引用方同步 → SHACL 报告是真实引擎产出（非硬编码）→ 界面切英文无中文残留（除领域内容外）。

### 3.2 任务清单（按依赖排序，可并行项标注）

**3.2.1 元数据库 + 迁移（H-00）· 后端 · 第 1 周**

| #   | 任务                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 产出                          | 验收                           |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- | ------------------------------ |
| 1   | 配置 `app/core/config.py`（pydantic-settings，DATABASE_URL 默认 SQLite dev，支持 PG）+ `app/core/logging.py`                                                                                                                                                                                                                                                                                                                                                                                | 配置加载与日志                | 单测覆盖配置加载               |
| 2   | `app/models/`：按上游 §6 落 ORM——资源与版本层（namespace / model_resource / resource_revision / resource_parent / resource_ref）、业务域层（business_domain / domain_revision / domain_member）、本体层（ontology / ontology_revision / ontology_resource_ref / canvas_layout）、映射层（mapping / mapping_revision / mapping_preview）、执行层（extraction_run / graph_revision / entity_identity / fact_evidence）、校验层（validation_report / validation_result）、隔离层（quarantine） | SQLAlchemy 2.0 ORM + 关系     | mypy strict 通过；表关系图自洽 |
| 3   | Alembic 初始化 + 首版迁移 `migrations/versions/0001_initial.py`                                                                                                                                                                                                                                                                                                                                                                                                                             | `alembic upgrade head` 可建库 | 迁移幂等，downgrade 可回滚     |
| 4   | `label_i18n` / `definition_i18n` JSONB 字段（PG）/ JSON（SQLite）+ `name` 唯一约束、`resource_revision` 只增、`graph_revision.is_current` 部分唯一索引                                                                                                                                                                                                                                                                                                                                      | DDL 约束                      | 约束在迁移脚本里，不靠应用层   |

**3.2.2 错误码体系 + 状态机 + 修订号（H-01/H-02）· 后端 · 第 1-2 周**

| #   | 任务                                                                                                                                                                                    | 产出                                | 验收                                                                           |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------ |
| 5   | `app/core/errors.py`：错误码注册表（`KG.REVISION_CONFLICT`、`KG.NOT_FOUND`、`KG.VALIDATION_VIOLATION`…），`KGError` 异常 + FastAPI exception handler 统一返回 `{code, params, message}` | 错误码清单 + handler                | 集成测试：抛 KGError → 响应体是错误码+参数，无中文句子                         |
| 6   | `app/domain/`：`status_service`（DRAFT/PUBLISHED 状态机）、`revision_service`（If-Match 校验、乐观锁冲突返回 409）                                                                      | 领域服务（不依赖 Web 框架，可单测） | 单测：并发写同一资源 → 一个 200 一个 409；发布即冻结，resource_revision 不可改 |
| 7   | `app/core/db.py`：session 工厂 + 事务边界（写接口包在事务里，失败回滚）                                                                                                                 | session 管理                        |                                                                                |

**3.2.3 命名空间与 IRI 治理（H-03）· 后端 · 第 2 周**

| #   | 任务                                                                                                                                                                                                   | 产出                                                    | 验收                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------- | --------------------------------------------------------------------- |
| 8   | `app/iri.py` 已有基础；补 `app/domain/iri_service.py`：namespace 注册（prefix/iri/protected）、IRI 生成、唯一性校验、**更名影响面扫描接口**（扫描关系端点、域成员、本体引用、映射——M2 映射落地后接入） | `/api/v1/namespaces` CRUD + `/api/v1/iri/impact?name=X` | 单测：改 Employee 名 → 引用方列表返回完整；protected namespace 不可删 |
| 9   | `app/api/v1/resources.py`（namespace 路由占位，M1 补资源 CRUD）                                                                                                                                        | 路由                                                    | openapi.json 生成                                                     |

**3.2.4 语义引擎 PoC（H-04）· 语义工程 · 第 2-3 周（关键路径，不通过不进 M1）**

| #   | 任务                                                                                            | 产出                               | 验收                                                         |
| --- | ----------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------ |
| 10  | `app/semantics/` 适配层：定义接口 `parse / serialize / validate / reason`，提供 `RdflibBackend` | 接口 + RdflibBackend               | 接口稳定，可替换实现                                         |
| 11  | 安装 `[semantic]` 依赖（rdflib/pyshacl/owlrl），Turtle 解析/序列化往返测试                      | 往返测试                           | Turtle → Graph → Turtle，IRI 与字面量不丢                    |
| 12  | **一个真实 SHACL shape 跑出真实报告**（用固定 Turtle 夹具，不硬编码结论）                       | `validate()` 返回 pySHACL 真实报告 | 报告结构与 pySHACL 输出一致；构造违反用例 → 报告含 violation |
| 13  | owlrl 最小推理（RDFS+OWL Horst）跑通；`reason()` 返回推断三元组                                 | 推理测试                           | owl:subClassOf 传递可推出                                    |
| 14  | （可选）`JenaRemoteBackend` 接口占位，M4 再接                                                   | 接口占位                           | 不阻塞 M1                                                    |
| 15  | A08 未接推理器时强制返回 `NOT_RUN`（四态不失真）                                                | 四态常量                           | 单测断言 NOT_RUN 不会被当 PASS                               |

**3.2.5 前端骨架（i18n L1 + Naive UI + 路由 + API 客户端）· 前端 · 第 1-3 周（与后端并行）**

| #   | 任务                                                                                                                                                 | 产出              | 验收                                    |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | --------------------------------------- |
| 16  | 安装 vue-router4 / pinia / vue-i18n9 / naive-ui / axios（或 fetch 封装）；`web/package.json` 加依赖（MIT 许可）                                      | 依赖就位          | 许可证检查通过                          |
| 17  | `web/src/i18n/index.ts`：vue-i18n 初始化，默认 zh-CN，备选 en-US，语言切换存 localStorage，未设置按 `navigator.language` 回退                        | i18n 装配         | 切换语言无中文残留（除领域内容）        |
| 18  | `web/src/locales/{zh-CN,en-US}.json`：补 app.title、菜单 6 项、四态码表、状态码表、错误码表骨架；ESLint `@intlify/vue-i18n/no-raw-text` 拦截裸文本   | 双语 key 集合一致 | i18n key 差异率 = 0（自定义脚本校验）   |
| 19  | `web/src/router/index.ts`：19 个路由占位（与 demo NAV 一一对应），hash/history 兼容；`web/src/views/*.vue` 全部占位组件                              | 路由可导航        | vue-tsc 通过；路由表与 demo 19 路由对齐 |
| 20  | `web/src/api/client.ts`：基于 openapi-typescript 生成的 `schema.d.ts` 封装类型化客户端（先接 health/iri 两个接口）；`pnpm` 或 npm script 生成 schema | 类型化客户端      | 前端调用 `/api/v1/health` 类型安全      |
| 21  | `web/src/stores/`：Pinia 装配；`web/src/App.vue`：Naive UI 主题（`themeOverrides` 对齐 demo `--pri:#315ee7`）+ 全局 layout（一级菜单占位）           | 应用外壳          | 启动 `make dev` 前端可见菜单骨架        |
| 22  | `web/src/styles/tokens.css`：CSS 变量，Naive UI 主题与自绘 SVG 画布共用                                                                              | 变量层            |                                         |

**3.2.6 CI 基线 + 开源化 · 后端/DevOps · 第 3 周**

| #   | 任务                                                                                                                           | 产出     | 验收                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------ | -------- | ------------------------ |
| 23  | `.github/workflows/ci.yml`：ruff + mypy + pytest + eslint + vue-tsc + build + i18n key 校验 + `pip-licenses` / license-checker | CI       | PR 触发全绿              |
| 24  | demo `tests/*.cjs` 断言迁移为 pytest（后端）+ Vitest（前端）骨架——M1/M3 再填实现                                               | 测试骨架 | 测试可运行（哪怕空断言） |
| 25  | NOTICE / CONTRIBUTING / PR-Issue 模板 / DCO（`Signed-off-by`）核验是否齐备，缺则补                                             | 治理文件 | 上游 §2.4                |

### 3.3 M0 验收清单

- [ ] 写一条 model_resource → 发布 resource_revision → 重启 uvicorn → 仍在库
- [ ] 并发写同一资源（带 If-Match）→ 一个 200 一个 409 `KG.REVISION_CONFLICT`
- [ ] 改 Employee 名 → `/iri/impact` 返回完整引用方（关系端点、域成员、本体引用）
- [ ] SHACL `validate()` 对构造的违反用例返回真实 violation 报告（非硬编码）
- [ ] `make dev` 前端菜单可见，切英文界面无中文残留（领域内容外）
- [ ] `make lint` + `make test` 全绿
- [ ] CI 全绿，含 i18n key 差异率 = 0

---

## 4. M1 · 语义资产闭环（5 周）★ 主流程上半段 S1-S4

### 4.1 目标

四类语义资产（实体类型 / 关系类型 / 业务域 / 本体）可建、可改、可版本化、可发布、可校验。覆盖 S1 建实体类型 → S2 建关系 → S3 组业务域 → S4 装配本体并发布。

### 4.2 任务清单

**4.2.1 实体类型库（H-11）· 后端+前端 · 第 1-2 周**

| #   | 任务                                                                                                                                                                                                                       | 产出            | 验收                           |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------------------------ |
| 1   | `app/models` 资源层已落（M0）；补 `app/domain/type_service.py`：实体类型 CRUD + 数据属性（IRI/名称/标签/定义/定义域/值域/数据类型+枚举引用）+ 类型层级（resource_parent）+ 引用统计 + 版本历史                             | 领域服务 + 单测 | A03 属性元数据无缺失           |
| 2   | `app/schemas/type.py`：Pydantic v2 请求/响应模型，`label_i18n`/`definition_i18n` 双语字段（L3 落地）                                                                                                                       | schema          | mypy strict                    |
| 3   | `app/api/v1/types.py`：实体类型路由（list/get/create/update/publish，写接口带 If-Match）                                                                                                                                   | openapi 更新    | 契约测试                       |
| 4   | 前端 `views/EntityTypeList.vue`：Naive UI `n-data-table`（虚拟滚动 + 树形 + 行内编辑骨架）+ 筛选 + 分页；`views/EntityTypeDetail.vue`：六页签（基本信息/数据属性/类型层级/关联关系/规则/引用与版本）用 `n-tabs`+`n-drawer` | 列表+详情页     | A01 类型多域引用指向同一资源   |
| 5   | i18n L3：实体类型编辑界面双语同屏编辑，中文必填、英文可选（缺失回退中文并标"未翻译"）                                                                                                                                      | 双语编辑        | 切英文，英文缺失字段标注未翻译 |

**4.2.2 关系类型库（H-12）· 后端+前端 · 第 2-3 周**

| #   | 任务                                                                                                                                                                                          | 产出            | 验收                                                                             |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | -------------------------------------------------------------------------------- |
| 6   | `app/domain/relation_service.py`：对象属性全字段（定义域/值域/基数注明约束方向/逆关系/OWL 特征：功能性/对称/传递）+ 多端点"或 vs 交集"表达；启用关系属性 → 生成关联实体 + 端点角色 + 属性清单 | 领域服务 + 单测 | A05 跨域与多类型关系表达正确；A06 同一对实体两次业务关系分别保存（关联实体模式） |
| 7   | OWL 特征兼容性校验（如对称+传递不兼容某些组合）→ 校验失败返回错误码                                                                                                                           | 校验逻辑        | 单测覆盖组合                                                                     |
| 8   | 前端 `views/RelationTypeList/Detail.vue`：关系编辑表单 + 关联实体模式切换                                                                                                                     | 页面            |                                                                                  |

**4.2.3 业务域管理（H-10）· 后端+前端 · 第 3 周**

| #   | 任务                                                                                                                                        | 产出            | 验收                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ---------------------------------------------------------- |
| 9   | `app/domain/domain_service.py`：目录树 + 成员增删 + **维护责任域唯一** + 跨域引用 + 发布前四项检查（成员存在性/版本可用/依赖完整/循环引用） | 领域服务 + 单测 | 发布前检查拦截不合法装配                                   |
| 10  | 前端 `views/BusinessDomain.vue`：`n-tree` 懒加载 + 成员清单抽屉                                                                             | 页面            | A02 域/继承/关系三分离（数据模型、操作入口、导出语义分开） |

**4.2.4 本体装配与设计器（H-13）· 后端+前端 · 第 3-4 周**

| #   | 任务                                                                                                                                                                                                                                                                           | 产出            | 验收                                         |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------- | -------------------------------------------- |
| 11  | `app/domain/ontology_service.py`：装配模板选型（带来源）→ 引用域/单类型（按 IRI 去重）→ 挂入/新建关系 → 校验 → 发布（冻结全部资源版本到 resource_versions 快照）；相同资源不兼容版本显式冲突、不静默覆盖；`canvas_layout`（scope+positions JSONB）单独存储、**不产生语义版本** | 领域服务 + 单测 | A04 发布固定版本；A17 拖动只改 canvas_layout |
| 12  | 前端 `views/OntologyDesigner.vue`：**全页可拖拽 SVG 画布**（移植 demo 布局算法：分区、贝塞尔折行、标签贪心避让、按 scope 存拖拽位置）；拖拽只 PATCH `canvas_layout`                                                                                                            | 画布组件        | A17 一张图看清全貌且节点拖动不产生语义版本   |

**4.2.5 资产层校验引擎（H-50）· 语义工程 · 第 4-5 周**

| #   | 任务                                                                                                                                                                                                                 | 产出     | 验收                                                      |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------- |
| 13  | `app/domain/validation_service.py` + `app/semantics`：8 项检查（资源闭包/类型元数据/属性值域/关系端点与关联实体/规则集/核心模板必需类型/命名空间与核心包锁定/OWL 一致性），**四态输出**（PASS/VIOLATION/NA/NOT_RUN） | 校验引擎 | A01~A06；A07 核心模板（M4 补标准条款来源）                |
| 14  | A01-A07 真实执行；**A08 未接推理器时强制 NOT_RUN**（复用 M0 的四态常量）                                                                                                                                             | 四态诚实 | 单测：A08 在无 Jena sidecar 时返回 NOT_RUN，绝不被当 PASS |

### 4.3 M1 验收清单

- [ ] A01 类型多域引用指向同一资源；A02 域/继承/关系三分离；A03 属性元数据无缺失；A04 发布固定版本；A05 跨域与多类型关系表达正确；A06 同一对实体两次业务关系分别保存；A17 拖动不产生语义版本
- [ ] demo `knowledge-graph.test.cjs` 布局断言迁移到 Vitest 通过
- [ ] i18n L3 双语字段在实体类型/关系/业务域落地，切英文领域标签可见
- [ ] `make lint` + `make test` 全绿，CI 全绿

---

## 5. M2 · 数据接入与映射（4 周）★ 主流程中段 S5-S6

### 5.1 目标

接一个真实只读库，采到元数据，配出一份能编译、能预览、可发布的映射方案。

### 5.2 任务清单

**5.2.1 样例库交付（M2 正式交付物，非附属）· 后端/测试 · 第 1 周**

| #   | 任务                                                                                                                                                                                                                                                           | 产出   | 验收                                                            |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------- |
| 1   | `samples/seed.sql` + `samples/enterprise_demo.sqlite`：必须覆盖三类结构——外键（`hr_employee.enterprise_id → enterprise.id`）、中间表（`ct_contract_project`）、带属性关联表（`pm_project_member`，含**同一对实体两条参与记录** 7001/7003，验证不被三元组去重） | 样例库 | 三类结构齐全；数据脱敏（example.org / 91310000DEMA000001 形式） |

**5.2.2 数据源连接（H-20）· 后端 · 第 1-2 周**

| #   | 任务                                                                                                                                                                                          | 产出          | 验收                         |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ---------------------------- |
| 2   | 引入 `cryptography`（M2 凭据加密，确认 wheel 可用，上游已知坑：Python 3.13 sdist 失败，用 3.12）；`app/core/crypto.py` AES-GCM，密钥来自环境变量                                              | 加密层        | 凭据密文落库，解密只在后端   |
| 3   | `app/connectors/`：SQLAlchemy dialects（SQLite/MySQL/PostgreSQL 只读强制）；`app/domain/source_service.py`：连接参数+凭据密文，浏览器只拿可展示字段；测试连接、读权限检查、只读强制、超时限制 | 连接器 + 服务 | A15 凭据仅后端使用、源库只读 |
| 4   | `app/api/v1/sources.py`：数据源 CRUD + 测试连接接口                                                                                                                                           | 路由          |                              |
| 5   | 前端 `views/DataSourceList.vue`：连接表单（凭据字段不回显明文）+ 测试连接按钮 + 状态                                                                                                          | 页面          |                              |

**5.2.3 元数据采集与数据目录（H-21）· 后端+前端 · 第 2-3 周**

| #   | 任务                                                                                                                                                                                                         | 产出            | 验收                           |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------- | ------------------------------ |
| 6   | `app/domain/metadata_service.py`：连接 → 库 → schema → 表/视图 → 字段（类型/可空/PK/FK/注释）采集；`metadata_snapshot` 结构快照 + 变更识别（diff 上一版）；反向引用"该表被哪些映射引用"（M2 映射落地后接入） | 采集服务 + 单测 | 变更识别能检出加列/删列/改类型 |
| 7   | 前端 `views/DataCatalog.vue`：`n-tree` 四级下钻 + 懒加载 + 字段详情 + 变更标记 + 反向引用面板                                                                                                                | 页面            |                                |

**5.2.4 受限样例预览（H-22）· 后端 · 第 2 周**

| #   | 任务                                                                                       | 产出     | 验收                         |
| --- | ------------------------------------------------------------------------------------------ | -------- | ---------------------------- |
| 8   | `app/domain/preview_service.py`：表/字段级样例，限行限时不入页面存储、不入导出包与运行日志 | 预览服务 | A15 密码不进入页面/导出/日志 |

**5.2.5 映射方案 + 5 步向导（H-30）· 后端+前端 · 第 3-4 周**

| #   | 任务                                                                                                                                                                                                                 | 产出            | 验收                                                             |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ---------------------------------------------------------------- |
| 9   | `app/domain/mapping_service.py`：mapping + mapping_revision（onto_version 冻结）；实体映射 + 关系映射三模式（外键/中间表/关联表）+ 字段转换 + 空值策略；发布冻结目标本体版本                                         | 领域服务 + 单测 | A10 一表生成多类型、一类型来自多表                               |
| 10  | 前端 `views/MappingWizard.vue`：5 步向导（目标本体/数据来源/映射设计/预览校验/保存运行）+ **图谱视图 ⇄ 表格视图同一份配置双视角**；图谱视图点节点配实体映射、点连线配关系映射，未配置项图上可见（橙色虚线框/灰虚线） | 向导            | A18 点节点配表、点连线配条件、未配置项图上可见、窄栏下拉不被压扁 |

**5.2.6 映射编译与预览校验（H-31）· 后端 · 第 4 周**

| #   | 任务                                                                                                                                    | 产出          | 验收                                                                             |
| --- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------- | -------------------------------------------------------------------------------- |
| 11  | `app/domain/mapping_compiler.py`：字段映射四列（目标属性→源字段→转换规则→空值策略）+ **样例实算结果**；关系三模式编译；错误定位到字段级 | 编译器 + 单测 | A11 空键/重复键/缺失端点/多匹配有明确结果；A12 日期/枚举/小数/空值按声明策略处理 |
| 12  | `mapping_preview` 落库；前端预览面板展示实算结果 + 字段级错误                                                                           | 预览          |                                                                                  |

### 5.3 M2 验收清单

- [ ] A10 表与类型多对多；A11 空键/重复键/缺失端点/多匹配明确；A12 日期/枚举/小数/空值按策略；A15 凭据仅后端、源库只读；A18 映射向导交互
- [ ] samples 样例库三类结构 + 两条参与记录齐全，`make seed` 可灌入
- [ ] `make lint` + `make test` 全绿

---

## 6. M3 · 抽取执行与图谱闭环（5 周）★ v0.1.0 P0 交付

### 6.1 目标

真实跑通抽取，产出可浏览、可检索、可追溯的图谱版本。**M3 结束 = 端到端主流程贯通 = P0 22 项全部上线 = 打 tag v0.1.0。**

### 6.2 任务清单

**6.2.1 抽取引擎四阶段（H-32）· 后端 · 第 1-2 周**

| #   | 任务                                                                                                                                                                                                  | 产出            | 验收                     |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------------------ |
| 1   | `app/domain/extraction_service.py`：四阶段——实体映射 → 关系映射 → 规则检查 → 隔离；**幂等**（重跑不重复累计，基于 entity_identity 稳定 IRI）；统计口径分离（三元组数 ≠ 实体数，派生边只用于查询加速） | 引擎 + 单测     | A13 任务可重放；幂等断言 |
| 2   | `extraction_run`（status/cfg 快照/fingerprint/counts/started/finished）+ `quarantine`（表/源键/字段/原值/转换链/命中规则）                                                                            | 运行记录 + 隔离 | A14 问题数据可定位       |
| 3   | 执行首版用 FastAPI 后台任务（asyncio + 运行记录落库 + 状态机），不引 Celery                                                                                                                           | 后台任务        |                          |

**6.2.2 图谱版本与结果集管理（H-33）· 后端 · 第 2-3 周**

| #   | 任务                                                                                                                                                                     | 产出            | 验收                                                               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------- | ------------------------------------------------------------------ |
| 4   | `graph_revision`（run_id/is_current/publishable）；当前结果集指针（`is_current` 部分唯一索引）；零产出判"失败"；**失败运行不覆盖上一成功版本**；可从任一历史成功运行切回 | 版本管理 + 单测 | A20 运行闭环：前置校验拦截、零产出判失败、失败不覆盖、结果集可切换 |
| 5   | `entity_identity` / `graph_triple` / `derived_edge` / `fact_evidence`（subject/predicate/object/source_id/table/row_key/columns[]/run_id/evidence_type）落库             | 三元组表        |                                                                    |

**6.2.3 图谱浏览（H-40）· 前端 · 第 3-4 周**

| #   | 任务                                                                                                                                                                                                                                 | 产出     | 验收               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ------------------ |
| 6   | 前端 `views/GraphBrowse.vue`：关系探索（以实例为中心分层展开 1/2 度）+ 全图概览（按业务域分区）；名称/类型/业务键/IRI 即时检索；直接关系实线、派生关系紫虚线；相同端点多条事实分开弯曲（**不因视觉去重丢证据**）——移植 demo 画布算法 | 图谱组件 | A17 一张图看清全貌 |
| 7   | 来源被隔离时明确提示"来源记录未进入当前候选图"，**不伪装为证据完整**                                                                                                                                                                 | 诚实提示 |                    |

**6.2.4 实体实例与详情（H-41）· 前端 · 第 3 周**

| #   | 任务                                                                                                                                          | 产出      | 验收 |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ---- |
| 8   | 前端 `views/EntityInstanceList.vue`：`n-data-table` 虚拟滚动 + 搜索；`EntityInstanceDetail.vue`：五页签（数据属性/出边/入边/派生边/来源证据） | 列表+详情 |      |

**6.2.5 来源证据追溯（H-42）· 后端+前端 · 第 4 周**

| #   | 任务                                                                                                                                          | 产出            | 验收            |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | --------------- |
| 9   | `app/domain/evidence_service.py`：每条事实摊平为 主体·谓词·客体·来源连接/表/行键/字段·运行批次·证据类型；从图谱任一三元组反查到 fact_evidence | 追溯服务 + 单测 | 8 步贯通第 7 步 |

**6.2.6 实例层校验报告（H-51）· 语义工程 · 第 4 周**

| #   | 任务                                                                                                                   | 产出 | 验收       |
| --- | ---------------------------------------------------------------------------------------------------------------------- | ---- | ---------- |
| 10  | `validation_report` + `validation_result`：规则命中清单 + 严重程度 + 焦点节点 + 可定位；四态统计（复用 M1 的四态常量） | 报告 | 四态不失真 |

**6.2.7 工作台（H-60）· 前端 · 第 5 周**

| #   | 任务                                                                                                               | 产出   | 验收 |
| --- | ------------------------------------------------------------------------------------------------------------------ | ------ | ---- |
| 11  | 前端 `views/Dashboard.vue`：建模与抽取整体进度 + 待处理问题直达 + 主流程各环节入口（ECharts 工作台统计，轻量使用） | 工作台 |      |

### 6.3 M3 贯通测试（8 步，必须一次跑完整条链）

| 步骤 | 动作                                                    | 通过条件                                          |
| ---- | ------------------------------------------------------- | ------------------------------------------------- |
| 1    | 在 samples 样例库上，从零建 3 个实体类型 + 2 个关系类型 | 全部发布成功，IRI 稳定                            |
| 2    | 组 1 个业务域，装配 1 个本体并发布                      | 校验 8 项无"违反"，A08 未接推理器则如实标"未执行" |
| 3    | 配 1 份映射（覆盖外键+中间表+关联表三模式）             | 预览阶段字段级样例实算正确                        |
| 4    | 运行抽取                                                | 产出实体/关系/派生边/隔离清单，计数可复现         |
| 5    | **重复运行**                                            | 实体 IRI 集合、关系数、隔离数完全一致（幂等）     |
| 6    | 制造失败运行（清空来源表）                              | 判"失败"，**当前结果集不变**，可切回上一成功运行  |
| 7    | 任取一条事实                                            | 反查到来源连接/表/行键/字段/运行批次              |
| 8    | 隔离记录                                                | 定位到表/源键/字段/原值/转换链/命中规则           |

### 6.4 M3 自动化回归（4 项断言，CI 必跑）

- 幂等：重复运行实体 IRI 集合、关系数、隔离数一致
- 失败保护：失败运行不改写 `is_current`
- 结果集切换：可切回历史成功运行且数据一致
- 证据追溯：任一三元组可反查到完整 fact_evidence

### 6.5 产出

打 tag `v0.1.0`，发布 Docker 镜像与在线 Demo（`make seed` 后 10 分钟走通全流程）。`docker-compose.yml`（server + web(nginx) + postgres）一体化部署。

---

## 7. 依赖关键链（不可乱序）

```
链 1 语义主干（M0→M1）
  H-00 元数据库 → H-01 后端 → H-02 状态机 → H-03 IRI 治理
                                        ↓
  H-11 实体类型 → H-12 关系 → H-10 业务域 → H-13 本体装配 → H-50 资产校验
                                                          ↑
                                                    H-04 语义引擎（M0 PoC 必须先过）

链 2 数据主干（M2，需 H-13 已发布本体）
  H-20 连接 → H-21 元数据/目录 → H-22 样例 → H-30 映射方案 → H-31 映射编译预览

链 3 执行主干（M3）
  H-31 → H-32 抽取引擎 → H-33 图谱版本 → H-40/H-41/H-42 图谱与证据
           ↑                    ↑
     H-04 + H-50          H-51 实例校验
```

三个"看似可后置、实则必须前置"的坑（上游 §3，本计划照搬）：

1. H-04 语义引擎 M0 内跑通，否则 H-50/H-32/M-01 全返工。
2. H-03 IRI 治理与 H-11 同期或更早，更名重算牵动四处。
3. H-33 图谱版本不能并进 H-32，"失败不覆盖""结果集指针"是产品语义不是执行细节。

---

## 8. 团队分工建议（按里程碑）

| 角色      | M0                   | M1                 | M2                          | M3                      |
| --------- | -------------------- | ------------------ | --------------------------- | ----------------------- |
| 产品/架构 | 契约定稿、验收口径   | 资产模型 review    | 样例库验收、映射交互 review | 8 步贯通验收            |
| 语义工程  | H-04 PoC（关键路径） | H-50 校验引擎      | 映射编译语义                | H-51 实例校验、规则阶段 |
| 后端 1    | 元数据库+迁移+错误码 | H-11/H-12 领域服务 | H-20/H-21 连接+元数据       | H-32 抽取引擎           |
| 后端 2    | 状态机+IRI 治理      | H-10/H-13 领域服务 | H-30/H-31 映射              | H-33 图谱版本           |
| 后端 3    | API 路由+CI          | 资产 API           | 数据接入 API                | 图谱/证据 API           |
| 前端 1    | 骨架+i18n+主题       | 实体类型/关系页面  | 映射向导                    | 图谱浏览（自绘 SVG）    |
| 前端 2    | 路由+API 客户端      | 业务域/本体设计器  | 数据目录                    | 实体实例/工作台         |
| 测试      | demo 断言迁移骨架    | A01-A06 断言       | samples 验收、A10-A12       | 8 步贯通 + 4 项回归     |

---

## 9. 风险与对策（沿用上游 §10，标注本计划内的应对时点）

| 风险                    | 本计划应对时点                                               |
| ----------------------- | ------------------------------------------------------------ |
| H-04 语义引擎延期       | M0 第 2-3 周关键路径，不通过不进 M1                          |
| H-03 IRI 治理被当小功能 | M0 第 2 周交付更名影响面扫描接口                             |
| i18n 只做界面           | M0 L1/L2 骨架，M1 L3 随资源模型落地                          |
| 版本冻结语义被简化      | M1 H-13 发布冻结 + M3 H-33 失败保护列为独立验收项            |
| 关系属性塞进三元组      | M1 H-12 关联实体模式，M3 贯通第 5 步验证两条参与记录分别保存 |
| 未执行被当通过          | 四态常量 M0 落地，M1 H-50 + M3 H-51 强制 NOT_RUN             |
| 样例库质量不足          | M2 第 1 周交付，三类结构 + 重复参与记录是硬指标              |
| cryptography on Py3.13  | M2 引入时确认 wheel，用 3.12                                 |
| 一人维护 47 项          | 严格 P0=22 项，M3 即对外发布 v0.1.0                          |
| 本机 HTTP 代理误判 502  | 测试连接 127.0.0.1 加 `--noproxy '*'`                        |

---

## 10. Definition of Done（v0.1.0 / M3）

1. P0 共 22 项全部上线，无占位按钮，无"只弹提示不干活"的控件
2. 9 个主流程环节在 samples 真实只读库上连续走通，全程不离开系统界面
3. M3 贯通测试 8 步一次性全通过
4. 幂等 / 失败保护 / 结果集切换 / 证据追溯四项有自动化断言
5. 校验报告未执行项如实标注（四态不失真）
6. 实测规模上限写入 README
7. 中英双语可切换（界面 + 消息 + 领域标签）
8. `make up` 单机一体可跑，含 seed 数据
9. `make lint` + `make test` + CI 全绿
